{
  "name": "tenner",
  "version": "1.0.0",
  "description": "The best freelance app",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/server.js --ignore client",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AngelMond/Tenner.git"
  },
  "author": "Moondragons",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/AngelMond/Tenner/issues"
  },
  "homepage": "https://github.com/AngelMond/Tenner#readme",
  "devDependencies": {
    "concurrently": "^5.1.0"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.1.2"
  }
}
